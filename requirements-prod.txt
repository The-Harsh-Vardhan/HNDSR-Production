# HNDSR Production Dependencies (pinned)
# Generated: 2026-02-22
# These are RUNTIME-ONLY dependencies for the production Docker image.
# Dev/test tools (pytest, black, flake8, etc.) are in requirements.txt.

# Core ML
torch==2.1.2
torchvision==0.16.2
numpy==1.26.3
pillow==10.2.0

# API / Serving
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# Monitoring
prometheus-client==0.19.0

# Utils
python-dotenv==1.0.0
pyyaml==6.0.1
