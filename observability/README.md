# HNDSR Observability

Monitoring and alerting configuration for the HNDSR production system.

## Components
- `prometheus.yml`: Metric scraping configuration.
- `grafana_dashboard.json`: Pre-configured dashboard for tracking throughput and latency.
- `alerting_rules.yml`: SLI/SLO-based alert definitions.
